{
  "name": "bluent-social",
  "version": "1.0.0",
  "description": "A simple and modern social app",
  "main": "index.js",
  "scripts": {
    "start": "ng serve",
    "build": "ng build",
    "build:android": "capacitor sync android && cd android && ./gradlew bundleRelease",
    "test": "ng test"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0",
    "@capacitor/android": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
